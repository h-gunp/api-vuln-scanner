# API Vulnerability Scanner 작업 지침

이 저장소에서 작업하는 Codex, Claude Code 등 모든 에이전트는 아래 규칙을 따른다.

## 우선순위와 범위

1. 사용자의 현재 요청을 가장 우선한다.
2. 이 파일의 규칙을 따른다.
3. 프로젝트 요구사항, API 명세, 일정, 업무 정책은 PM이 제공하는 확정 자료만 사용한다.

PM 자료가 아직 없거나 필요한 요구사항이 불명확하면, 기능·정책·계약을 추측해서 만들거나 문서화하지 않는다. 필요한 정보를 짧게 밝히고 사용자에게 확인한다.

## 소통

- 기본 응답 언어는 한국어로 한다.
- 명령어, 경로, 코드 식별자, 오류 메시지는 원문 영어를 유지한다.
- 결과부터 간결하게 말하고, 변경한 파일과 검증 결과를 함께 보고한다.
- 사용자가 분석·검토만 요청했을 때는 파일을 수정하지 않는다.

## 작업 방식

- 작업 전에는 현재 브랜치, Git 상태, 기존 파일과 관련 구현을 먼저 확인한다.
- 요청 범위 안에서 가장 작은 변경으로 해결하고, 관련 없는 리팩터링·의존성 추가·파일 재구성은 하지 않는다.
- 기존의 코드 스타일과 구조를 우선 따른다.
- 요구사항이나 계약이 불명확하면 구현 전에 확인한다. 임의의 API, 데이터 구조, 화면 정책을 추가하지 않는다.
- 검증 가능한 변경은 변경 범위에 맞는 테스트·린트·빌드·정적 검사를 실행한다. 실행하지 못한 검증은 이유와 영향을 명확히 보고한다.

## PM 자료와 문서

- 에이전트는 PM 역할의 요구사항 문서, API 명세, 일정 문서, 업무 정책 문서를 새로 만들거나 내용을 보완하지 않는다.
- PM이 올린 자료가 있으면 그것을 구현의 기준으로 사용한다.
- 구현에 필요한 PM 자료가 없으면 이를 차단 사유로 보고하고, 추정으로 진행하지 않는다.

## 안전과 버전 관리

- 비밀값, 토큰, API 키, 실제 개인정보를 코드·문서·로그에 넣지 않는다.
- 삭제, 덮어쓰기, 대량 변경처럼 복구가 어려운 작업은 대상과 영향을 먼저 확인한다.
- 커밋, 푸시, 브랜치 생성·삭제, 리베이스, PR 생성·수정은 사용자가 명시적으로 요청했을 때만 수행한다.
- 현재 작업 트리의 사용자가 만든 변경은 보존하며, 요청과 관계없는 변경을 되돌리거나 정리하지 않는다.

## 완료 보고

완료 시 다음을 짧게 알린다.

1. 무엇을 변경했는지
2. 변경한 파일
3. 실행한 검증과 결과
4. 남은 제약, 미검증 항목, 또는 사용자 확인이 필요한 사항
